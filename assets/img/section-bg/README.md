# 专区背景资产规范

桌面横向背景位于 `source/`，移动端竖向背景位于 `mobile/`。PNG 是保留的可编辑源图；网站和 OS 运行时加载同名 WebP 优化产物，不得覆盖或删除 PNG 源图。当前 WebP 统一使用质量 84 生成，12 张资源由 20.9 MB 降至约 1.07 MB。

## 当前映射

| 专区 | 文件 | 标志性元素 |
| --- | --- | --- |
| 主页 | `home.png` | 明亮的蓝青渐变、稀疏网络节点与克制的紫色边缘光，中心安静 |
| AI | `ai.png` | 按当前综合性能分级的 LLM 模型标志星图；Claude、GPT、Kimi 为最大三档，Gemini、Grok 等次之，GLM、Qwen、DeepSeek、MiniMax、豆包居中，其余模型作为边缘节点；Z.ai 使用官方黑底白 Z，Grok 使用黑洞标志，Meta/Muse 仅作第四梯队小节点 |
| COC | `coc.png` | 可辨识的野蛮人、石墙、火把、村庄防御轮廓 |
| 英语 | `english.png` | 书本、纸页、语言线条与字母意象 |
| 游戏 | `game.png` | 卡牌、奖杯与轻量竞技场轮廓 |
| 天择OS | `os.png` | 窗口框、终端光标和简洁桌面网格 |

## 新增专区检查表

1. 用 ImageGen 分别生成约 16:9 的桌面横图和 9:16 的移动端竖图，画面必须包含该专区独有且可辨认的标志性元素。
2. 桌面版将主体放进不易被首屏内容覆盖的边缘安全区；移动版将主体放进中央 85% 安全区。禁止在背景中生成正文、菜单或水印。
3. 分别保存为 `source/<zone>.png` 与 `mobile/<zone>.png`，再生成同名质量 84 WebP；在 `main.js` 的 `initTzSectionIdentity()` 登记专区，并在 `style.css` 同时配置桌面和移动 WebP 变量。
4. 必须实际截图检查 1440×900、390×844 两种视口；标志性元素应在页面内容叠加后仍可辨认，正文对比度必须足够。
5. 若专区也进入天择OS，同时检查 `PRESET_APPS` 与 `TZ_TREE`。

背景仅表达专区身份，不承担导航和文字信息；品牌 Logo 只作为环境标志使用，不伪造官方联名或背书。
